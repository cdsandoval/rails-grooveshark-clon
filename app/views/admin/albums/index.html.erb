<h1>List of Albums</h1>
<table>
    <tr>
        <thead>
            <th>Title</th>
            <th>Rating</th>
        </thead>
    </tr>
    <%@albums.each do |album| %>
    <tr>
        <td><%= albums.title%></td>
        <td><%= albums.rating%></td>
        <td>
            <%= link_to "Details", admin_album_path(song) %>
            <%= link_to "Update", edit_admin_album_path(song)%>
            <%= link_to "Delete", admin_album_path(song), method: :delete, data: { confirm: "Really delete this album?" } %>
        </td>
    </tr>
    <%end%>
</table>


<%= link_to 'New Album', new_admin_album_path %>